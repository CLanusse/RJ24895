


export const MainContent = ( {titulo, text} ) => {

    // const {titulo, text} = props
    
    return (
        <main>
            <h2>{titulo}</h2>
            <p>{text}</p>
        </main>
    )
}

// export default MainContent